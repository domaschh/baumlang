val = [1,2,3]

struct Tuple = Int -> Int

map = Int -> Int -> List -> List
map fx list = fx(head(list))++map(fx, tail(list))
map a b c = c

sum = Int -> Int -> Int
sum a b = a + b

print(map(1,2,val))
print(map(sum(3), val)