let a = [1,2,3]

let simplefunc = |a,b| {
    if a > b {
      1
    } else {
      2
    }
}

let map = |f,l|{
    if len(l) == 0 {
        []
    } else {
        concat(f(head(l)), map(f, tail(l)))
    }
}

let plus1 = |a| {
    a + 1
}

print(map(plus1, a))